# Local environment snapshot — 2026-06-25

```text
Compiler: GCC 13.3.0
CMake: 4.3.1
oneDNN: local apt package extraction detected at `third_party/onednn-local`
oneDNN runtime smoke: 3.1.1
TBB: headers detected, runtime library not detected via ldconfig
```

Implication: oneDNN smoke tests can now be compiled through the optional `FLASHONE_ENABLE_ONEDNN` CMake path. Continue keeping oneDNN optional so the reference path remains portable.
